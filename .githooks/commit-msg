#!/bin/sh
# Reject commit messages that do not reference a GitHub issue like #123
MSG_FILE="$1"
if grep -q -E "#\d+" "$MSG_FILE"; then
  exit 0
else
  echo "ERROR: Commit message must reference an issue (e.g., 'Closes #123' or 'Refs #123')." >&2
  exit 1
fi
