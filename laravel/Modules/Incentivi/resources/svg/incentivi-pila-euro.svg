<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" class="w-6 h-6">
  <style>
    @keyframes bounce {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-5px); }
    }
    .stack {
      fill: currentColor;
      transform-origin: center;
      transition: all 0.3s ease;
    }
    svg:hover .stack {
      animation: bounce 0.6s infinite;
    }
    svg:hover {
      transform: scale(1.1);
      transition: transform 0.3s ease;
    }
  </style>
  <rect class="stack" x="4" y="4" width="16" height="2"/>
  <rect class="stack" x="4" y="8" width="16" height="2"/>
  <rect class="stack" x="4" y="12" width="16" height="2"/>
  <rect class="stack" x="4" y="16" width="16" height="2"/>
  <text x="12" y="20" font-size="8" text-anchor="middle" fill="#fff">€</text>
</svg> 