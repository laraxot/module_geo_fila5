---
description: Laravel Boost — stub on-demand (il monolite era ~143k righe e causava Compaction exhausted).
alwaysApply: false
globs: laravel/**/*.php
---

# Laravel Boost (stub)

Le linee guida complete erano in un singolo file **monolitico** con `alwaysApply: true` (~143k+ righe) e **saturavano il contesto Cursor** → errore «Compaction exhausted».

- **Backup locale (non caricato come rule):** `laravel/.cursor/laravel-boost-guidelines.FULL.mdc.bak` — consultare solo a chunk (`Read` con `offset`/`limit`) se serve un estratto preciso.
- **On-demand:** `docs/wiki/rules/00-TRIGGER_MAP.md`, wiki modulo Xot, `qmd search "<topic>" --limit 5`.

Non ripristinare il monolite come `.mdc` in questa cartella senza ridimensionarlo drasticamente.
