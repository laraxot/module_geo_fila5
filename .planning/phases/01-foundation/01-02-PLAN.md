---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - "laravel/Modules/Gdpr/app/Actions/*.php"
  - "laravel/Modules/Gdpr/app/Http/Controllers/*.php"
  - "laravel/Modules/Gdpr/resources/views/*.blade.php"
autonomous: false
requirements:
  - AUDT-03
---

<objective>
**What this accomplishes:** GDPR-compliant data export endpoint
**Purpose:** Users can export all their personal data (DPIA requirement)
**Output:** API endpoint returning complete data package in JSON/ZIP format
</objective>

<execution_context>
@.planning/research/SUMMARY.md
@.agents/skills/laraxot-core/SKILL.md
@.agents/skills/laraxot-model-rules/SKILL.md
</execution_context>

<context>
## GDPR Export Requirements

Per GDPR Article 15 (Right of Access):
- User can request copy of all personal data
- Must include data from ALL related records (evaluations, indemnities, attendance)
- Must be machine-readable format
- Must be provided within 30 days

Research flag: Verify GDPR export includes all related records.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit existing GDPR module</name>
  <files>
    - "laravel/Modules/Gdpr/**/*.php"
    - "laravel/Modules/Gdpr/**/*.blade.php"
  </files>
  <action>
    1. Read current GDPR module structure
    2. Check if ExportAction or similar exists
    3. Document what data is currently exported
    4. Identify missing related records (evaluations, indemnities, attendance)
    5. Create list of all modules that store user-related data
  </action>
  <verify>`ls laravel/Modules/Gdpr/app/Actions/` shows export action</verify>
  <done>GDPR export gap analysis complete</done>
</task>

<task type="auto">
  <name>Task 2: Implement comprehensive GDPR export action</name>
  <files>
    - "laravel/Modules/Gdpr/app/Actions/ExportUserDataAction.php"
  </files>
  <action>
    Create `ExportUserDataAction` that collects ALL user data:
    
    1. User profile (name, email, department, role)
    2. Performance evaluations (all historical)
    3. Indemnities (all calculations and payments)
    4. Attendance records (presenze/assenze)
    5. Career progression history
    6. Any other module-specific data
    
    Pattern:
    ```php
    class ExportUserDataAction extends Action
    {
        public function execute(User $user): DataPackage
        {
            return new DataPackage([
                'user' => $this->exportUser($user),
                'evaluations' => $this->exportEvaluations($user),
                'indemnities' => $this->exportIndemnities($user),
                'attendance' => $this->exportAttendance($user),
                // ...
            ]);
        }
    }
    ```
  </action>
  <verify>Action can be called with user and returns complete package</verify>
  <done>GDPR export action created with all related records</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>GDPR export functionality</what-built>
  <how-to-verify>
    1. Create test user with sample data
    2. Call export action: `php artisan tinker "app(ExportUserDataAction::class)->execute(User::first())"`
    3. Verify output contains all data categories
    4. Check JSON structure is valid
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Create API endpoint and Filament page</name>
  <files>
    - "laravel/Modules/Gdpr/app/Http/Controllers/*.php"
    - "laravel/Modules/Gdpr/routes/web.php"
  </files>
  <action>
    1. Create API endpoint: GET /api/gdpr/export
       - Authenticated user can only export own data
       - Returns JSON or triggers ZIP download
    2. Create Filament page for HR admin to trigger exports
       - List export requests
       - Download as admin
    3. Add authorization policies
  </action>
  <verify>`curl -H "Authorization: Bearer $TOKEN" /api/gdpr/export | jq keys`</verify>
  <done>GDPR export endpoint functional and accessible</done>
</task>

</tasks>

<verification>
- [ ] Export action exists and is comprehensive
- [ ] API endpoint returns valid JSON with all user data
- [ ] Filament page for admin management
- [ ] Authorization prevents unauthorized access
- [ ] Response time < 30s for large datasets
</verification>

<success_criteria>
- **AUDT-03:** System maintains GDPR-compliant data export - VERIFIED
- User can export all personal data in machine-readable format
- Export includes evaluations, indemnities, attendance records
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-gdpr-export-SUMMARY.md`
</output>
