REGOLA OPERATIVA PERMANENTE: LEAFLET SI COLLEGA AL CONTAINER TRAMITE CLASSI LOCALI, MAI TRAMITE ID GLOBALI.
REGOLA OPERATIVA PERMANENTE: LATITUDELONGITUDEINPUT DEVE ESTENDERE XOTBASEFIELD, MAI COORDINATEPICKER.

bmad-create-story Devi creare un campo personalizzato per Filament v5 chiamato MapPicker da usare nelle Resource che
   gestisce una mappa interattiva basata su Leaflet incapsulata in un Web Component Lit riutilizzabile. Il campo deve
   integrarsi con Livewire e Alpine già presenti nel progetto Laravel e deve lavorare con due colonne reali del
   database chiamate latitude e longitude senza usare JSON. Il componente deve mostrare una mappa con marker draggable
   e aggiornare in tempo reale i valori di latitudine e longitudine sia negli input visibili sia nei campi Livewire
   tramite entangle separato per latitude e longitude. Se i valori iniziali di latitude e longitude sono null il
   componente deve automaticamente usare la geolocalizzazione del browser per centrare la mappa e posizionare il
   marker. Quando l’utente sposta il marker la posizione deve aggiornare Alpine state, gli input e Livewire senza loop
   o desincronizzazioni. Il Web Component Lit deve essere completamente isolato e gestire la logica della mappa,
   inclusa inizializzazione Leaflet, marker draggable e dispatch di eventi custom coords-changed verso Alpine. Il
   componente deve supportare fullscreen toggle che espande la mappa a schermo intero e deve gestire correttamente il
   resize di Leaflet con invalidateSize. Deve includere anche layer base mappa stradale e layer satellitare o
   equivalente tramite tile provider compatibile con Leaflet con possibilità di switch tra layer. Alpine deve fungere
   da orchestratore tra Livewire e Web Component usando @entangle live su latitude e longitude e watcher per
   sincronizzare i valori verso il Web Component. Il campo Filament deve essere riutilizzabile nelle Resource con
   sintassi MapPicker::make senza campo JSON ma collegandosi esplicitamente a latitude e longitude tramite metodi
   dedicati latitude() e longitude() nel componente custom. Il sistema deve essere progettato in modo modulare e
   inserito nel modulo GEO dell’applicazione, rispettando l’architettura a moduli del progetto, senza contaminare altri
   domini. Il lavoro non è considerato completo finché tutto il codice non passa senza errori e senza warning
   attraverso PHPStan, PHP Mess Detector (PHPMD), PHP Insights e Pest con test automatizzati. Deve essere inclusa una
   validazione visiva dei dati nel frontend: si consiglia l’uso di indicatori di stato inline (input validation
   colorata con error state), feedback immediato sullo stato delle coordinate e opzionalmente reverse geocoding per
   mostrare un indirizzo leggibile quando le coordinate cambiano, usando servizi basati su OpenStreetMap come Nominatim
   o Photon.


   bmad-create-story dopo aver studiato a fondo https://filamentphp.com/docs/5.x/forms/custom-fields, https://livewire.laravel.com/docs/properties#data-binding, https://livewire.laravel.com/docs/javascript#the-wire-object e https://lit.dev/
   Agisci come un senior architect specializzato in Laravel 12, Filament v5, Livewire v3 e Web Components con Lit, con esperienza reale in sistemi complessi e alta attenzione a performance, DX e manutenibilità, analizza profondamente un custom field Filament chiamato MapPicker che integra Leaflet tramite un web component Lit e sincronizza coordinate geografiche con Livewire, il tuo obiettivo è rifattorizzare l’intero sistema (PHP Field, Blade view, Alpine layer, integrazione Livewire JS e componente Lit) per ottenere un’architettura pulita, coerente e senza side effects, devi rispettare rigorosamente i principi ufficiali della documentazione Filament sui custom fields (uso corretto di $getStatePath, $applyStateBindingModifiers, gestione dello state come singola fonte di verità), della documentazione Livewire (uso corretto di $wire.$set, $wire.$watch, evitare uso improprio di entangle e loop reattivi) e della documentazione Lit (Shadow DOM, gestione styles tramite CSSResult, separazione UI/logic), individua e correggi tutte le criticità architetturali tra cui sincronizzazione bidirezionale fragile, duplicazione dello stato tra location e latitude/longitude, uso eccessivo di flag come _isExternalUpdate e _isSyncingFromExternal, potenziali memory leak (event listener globali non rimossi), coupling tra Blade e struttura dello state tramite string manipulation, e problemi di lifecycle tra Lit e Livewire, definisci chiaramente un contratto dati unico e stabile dove il field Filament gestisce uno state strutturato location { latitude, longitude } e il model finale riceve latitude e longitude solo in fase di submit tramite trasformazione esplicita, assicurati che il componente Lit sia completamente stateless rispetto a Livewire e funzioni solo come UI emitter tramite eventi custom (es. coords-changed), riduci al minimo la logica difensiva in shouldUpdate e sposta la responsabilità di sincronizzazione nel layer Alpine/Livewire, migliora la gestione delle performance evitando re-render inutili e garantendo che aggiornamenti di coordinate non generino loop, proponi una gestione pulita del CSS tramite file separato che esporta css`` per Lit mantenendo isolamento Shadow DOM, valuta se spostare logiche asincrone come geocoding verso metodi Livewire esposti (ExposedLivewireMethod) per maggiore controllo e testabilità, assicurati che il codice finale sia DRY, SOLID, altamente leggibile e modulare, documenta chiaramente ogni scelta architetturale e motiva ogni refactor con riferimento ai principi delle librerie utilizzate, produci come output finale codice pronto produzione per MapPicker.php, map-picker.blade.php e map-picker-lit.js coerenti tra loro, senza hack, senza workaround fragili e senza dipendenze implicite tra layer, con particolare attenzione a evitare race conditions tra init della mappa e aggiornamenti dello stato, mantenere compatibilità con Filament Wizard e garantire una UX fluida senza glitch visivi o lag nelle interazioni con la mappa


   bmad-create-story Agisci come un senior architect specializzato in Laravel 12, Filament v5, Livewire v3/v4 compatibility, Alpine.js, Web Components con Lit e Leaflet, con esperienza reale in sistemi modulari complessi, DX, performance, accessibilità, testabilità e clean architecture, e prima di scrivere anche una sola riga di codice studia a fondo e cita internamente nella tua analisi le fonti ufficiali e verificate su cui devi basarti: https://filamentphp.com/docs/5.x/forms/custom-fields
 https://filamentphp.com/docs/5.x/forms/overview
 https://livewire.laravel.com/docs/4.x/javascript
 https://livewire.laravel.com/docs/4.x/properties
 https://livewire.laravel.com/docs/4.x/wire-model
 https://lit.dev/docs/
 https://lit.dev/docs/components/properties/
 https://lit.dev/docs/components/events/
 https://lit.dev/docs/components/styles/
 https://leafletjs.com/reference.html
 https://leafletjs.com/examples/quick-start/
 e devi inoltre usare LLM Wiki come fonte di contesto e di memoria di progetto per documentare, accumulare conoscenza e guidare il refactor; se non sai esattamente cosa sia LLM Wiki devi prima studiarlo seriamente su internet con fonti aggiornate e verificate, capire il pattern, il protocollo, l’eventuale AGENTS.md, il workflow di compilazione della conoscenza, le convenzioni di organizzazione della wiki, verificare che sia tutto a posto per usarlo al meglio nel progetto e poi usarlo davvero per aggiornare e consolidare la documentazione tecnica e il ragionamento architetturale, studiando in particolare anche https://github.com/Pratiyush/llm-wiki
 https://github.com/nvk/llm-wiki
 e se trovi altre fonti autorevoli e più aggiornate devi includerle nel tuo processo; il tuo obiettivo è progettare, documentare, rifattorizzare e implementare un campo personalizzato Filament v5 chiamato idealmente LocationPicker come naming architetturalmente corretto secondo la documentazione ufficiale Filament, ma compatibile con l’uso nel progetto anche come MapPicker se il naming pubblico va mantenuto, da usare nelle Resource e nei Wizard del progetto modulare Laravel, inserito nel modulo Geo senza contaminare altri domini e senza introdurre coupling fragile, che integri una mappa interattiva basata su Leaflet incapsulata in un Web Component Lit riutilizzabile, mantenendo Alpine come glue layer minimale e Livewire come source of truth server-side, evitando hack, workaround fragili e side effects; devi prima analizzare in profondità l’implementazione esistente PHP Field, Blade view, Alpine layer, integrazione Livewire JS, componente Lit, lifecycle Leaflet e schema dati, trovare tutti i problemi reali e motivarli tecnicamente: duplicazione dello stato, coupling tra Blade e sibling paths latitude/longitude, uso improprio di entangle, sincronizzazione bidirezionale fragile, flag difensivi inutili come _isExternalUpdate e _isSyncingFromExternal, potenziali memory leak da listener globali non rimossi, shouldUpdate usato come toppa, re-render inutili, race conditions tra init della mappa e sync esterno, problemi di resize/fullscreen, testabilità scarsa, mancanza di un contratto dati stabile, documentazione insufficiente, e qualsiasi mismatch rispetto alle best practice ufficiali di Filament, Livewire, Alpine, Lit e Leaflet; devi poi decidere e motivare l’architettura finale migliore tenendo conto che il progetto attuale usa due colonne reali del database chiamate latitude e longitude, ma che per il layer di form la soluzione più pulita può essere un unico state del field chiamato location con struttura minima e stabile, lasciando la trasformazione verso latitude e longitude solo nel submit o nella hydration/dehydration del form se questo migliora robustezza e manutenibilità, e devi spiegare in modo rigoroso vantaggi e svantaggi delle due strategie, scegliendone una sola in modo coerente; se scegli la strategia moderna e più pulita devi fare in modo che il field Filament lavori come singola fonte di verità sul proprio $getStatePath(), usando correttamente $applyStateBindingModifiers solo dove serve e privilegiando il bridge esplicito tramite $wire.$set e $wire.$watch secondo la documentazione ufficiale del $wire object, evitando loop reattivi e mantenendo la UI snella; il Web Component Lit deve essere realmente UI-only, con Shadow DOM, proprietà ben definite, eventi custom semantici come location-changed o coords-changed, CSS isolato e organizzato in modo clean, preferibilmente tramite file separato che esporta css`` o equivalente robusto, senza conoscere direttamente né Livewire né Filament né PHP; Alpine deve fungere solo da adapter sottilissimo tra field state e web component, senza logica business, senza duplicazioni, senza stato ridondante, senza watcher superflui, senza normalizzazioni verbose inutili, e se una parte può essere rimossa per aderire a DRY e KISS va rimossa; devi implementare la mappa con marker draggable, click-to-set, corretto update marker/view, gestione resize, fullscreen toggle robusto con invalidateSize, supporto a layer stradale e satellitare o equivalente se davvero utile e sostenibile, ma ogni feature extra deve essere valutata in base al suo costo architetturale e può essere esclusa se non essenziale alla versione minimale; se latitude e longitude iniziali sono null devi valutare se usare o no geolocalizzazione automatica del browser, ma devi motivare la scelta con attenzione perché può introdurre loop, UX inattesa, permessi browser e fragilità, quindi non implementarla automaticamente solo perché “sembra comoda”: devi decidere in base a robustezza, UX e controllo esplicito dell’utente; per il geocoding e reverse geocoding devi valutare con rigore se lasciarli fuori dalla versione minimale oppure se implementarli come capability opzionale, idealmente lato server o tramite metodi del field esposti in modo pulito se la documentazione Filament lo consente, evitando fetch disordinati e logica distribuita senza controllo; devi produrre documentazione tecnica eccellente nel modulo Geo, negli eventuali temi coinvolti e nell’LLM Wiki del progetto, aggiornando indici, pagine correlate, rationale architetturale, contratto dati, eventi del componente, decisioni su Livewire/Alpine/Lit, flussi di submit/hydration, guida di utilizzo nelle Resource e nei Wizard, troubleshooting, limiti noti, rischi e possibili estensioni, aggiungendo backlink e cross-link utili; se nel progetto esistono docs locali, indici, wiki o regole operative devi leggerli prima, aggiornarli coerentemente e usarli come fonte primaria del contesto progettuale insieme a LLM Wiki; devi anche verificare se nel progetto esistono regole, memories, skills, documentazione nei moduli o nei temi, convenzioni di naming, standard di phpstan/phpmd/phpinsights/pest, e rispettarli aggiornando ciò che serve alla fine del lavoro; il codice finale deve essere production-ready, ultra DRY, ultra KISS, ultra clean code, leggibile, modulare, tipizzato, difendibile in code review, e deve includere almeno MapPicker o LocationPicker PHP class, Blade view del field, web component Lit, eventuale file styles separato, eventuali helper o test necessari, tutto coerente tra loro e senza dipendenze implicite; non devi fermarti finché l’implementazione non è coerente, documentata, testata e allineata alle fonti ufficiali, e devi validarla con PHPStan, PHPMD, PHP Insights e Pest senza warning o errori, oltre a controlli manuali reali del comportamento in frontend; infine devi spiegare con chiarezza perché ogni scelta è stata fatta, cosa è stato scartato, quali alternative erano possibili, perché l’uso di Lit ha o non ha senso rispetto ad Alpine-only in questo caso, quali compromessi sono stati accettati, e come il risultato finale massimizza robustezza, riuso, semplicità e manutenibilità nel tempo.

Agisci come un senior architect e senior full-stack engineer specializzato in Laravel 12, Filament v5, Livewire v4, Alpine.js, Lit 3, Leaflet, clean architecture, moduli Laraxot e code quality enterprise, e crea da zero un custom field Filament riutilizzabile chiamato CoordinatePicker per il modulo Geo del progetto, assumendo che tu non sappia nulla del progetto se non ciò che sto per dirti, quindi devi seguire rigorosamente queste istruzioni, studiare bene le fonti ufficiali e implementare tutto in modo production-ready, ultra DRY, ultra KISS, ultra clean code, tipizzato, difendibile in code review, con responsabilità ben separate tra PHP field, Blade view, Alpine bridge, web component Lit e Leaflet; prima di scrivere codice studia attentamente queste fonti ufficiali e usale come riferimento primario per le scelte architetturali e implementative: Filament custom fields https://filamentphp.com/docs/5.x/forms/custom-fields
 Filament forms overview https://filamentphp.com/docs/5.x/forms/overview
 Livewire 4 JavaScript https://livewire.laravel.com/docs/4.x/javascript
 Livewire 4 properties https://livewire.laravel.com/docs/4.x/properties
 Livewire 4 wire:model https://livewire.laravel.com/docs/4.x/wire-model
 Lit overview https://lit.dev/docs/
 Lit reactive properties https://lit.dev/docs/components/properties/
 Lit events https://lit.dev/docs/components/events/
 Lit styles https://lit.dev/docs/components/styles/
 Leaflet reference https://leafletjs.com/reference.html
 Leaflet quick start https://leafletjs.com/examples/quick-start/
 e se qualche punto ti è poco chiaro cerca e verifica anche le issue/limitazioni reali di Leaflet con Shadow DOM prima di prendere decisioni tecniche sul render root del web component; il progetto usa convenzioni Laraxot, quindi devi rispettare rigorosamente namespace, path e separazione dei layer: il namespace PHP corretto deve essere Modules\Geo\Filament\Forms\Components e non Modules\Geo\Forms\Components, il file PHP del field deve stare in laravel/Modules/Geo/app/Filament/Forms/Components/CoordinatePicker.php, la view Blade del field deve stare in laravel/Modules/Geo/resources/views/filament/forms/components/coordinate-picker.blade.php e la property $view del field deve essere geo::filament.forms.components.coordinate-picker, il web component Lit deve stare in un file JS separato sotto laravel/Modules/Geo/resources/js/components/coordinate-picker-field.js o path equivalente coerente col progetto, senza mischiare codice Filament-specific nel JS del web component; devi costruire un campo Filament v5 chiamato CoordinatePicker da usare nelle Resource e nei Wizard, con sintassi CoordinatePicker::make('coordinates'), che gestisce internamente uno stato composito del field del tipo { latitude: float|null, longitude: float|null }, ma non salva JSON nel database: il database reale continua ad avere due colonne separate latitude e longitude e il field deve essere dehydrated(false), con mapping esplicito finale verso le colonne reali tramite una utility statica extractCoordinates(array $data, string $fieldName = 'coordinates', string $latitudeColumn = 'latitude', string $longitudeColumn = 'longitude'): array; il field PHP deve estendere Filament\Forms\Components\Field, deve avere configurazione fluente pulita e tipizzata almeno per latitudeColumn(), longitudeColumn(), zoom(), center(), reverseGeocoding(), getters corrispondenti getLatitudeColumn(), getLongitudeColumn(), getZoom(), getLatitude(), getLongitude(), hasReverseGeocoding(), deve valorizzare lo state iniziale del field in afterStateHydrated() leggendo prima eventuali colonne reali del record e altrimenti eventuali coordinate di centro configurate, deve usare dehydrateStateUsing solo se davvero necessario ma preferibilmente restare con dehydrated(false) e mapping finale esplicito, deve includere #[ExposedLivewireMethod] su reverseGeocode(float $latitude, float $longitude): ?string e, dato che il metodo restituisce solo un dato a JavaScript senza richiedere re-render Livewire, deve usare anche #[Renderless], e il reverse geocoding deve essere server-side opzionale via Nominatim con timeout prudente e User-Agent valorizzato, handling robusto degli errori e ritorno null in caso di fallimento; il Blade field deve usare x-dynamic-component con :component="$getFieldWrapperView()" e :field="$field", leggere lo state del field con $getState() e i metadati minimi con $getStatePath(), $getKey(), $getZoom(), $hasReverseGeocoding(), costruire una config JSON minimale da passare ad Alpine, usare Alpine solo come bridge sottilissimo e non come secondo source of truth UI, quindi Alpine deve fare solo queste cose: trovare il custom element nel DOM, passargli zoom e coordinate iniziali, ascoltare $wire.$watch(config.statePath, ...) per eventuali aggiornamenti server-side, ascoltare l’evento custom coords-changed emesso dal web component, chiamare this.$wire.$set(config.statePath, { latitude, longitude }, false) usando l’API documentata di Livewire v4, e opzionalmente chiamare this.$wire.callSchemaComponentMethod(config.fieldKey, 'reverseGeocode', { latitude, longitude }) per valorizzare un readout testuale dell’indirizzo; non devi mettere in Alpine uno stato UI ridondante che duplica inutilmente quello del web component, quindi puoi tenere solo lat, lng, address per il readout e per il bridge, ma non devi spostare nel bridge responsabilità di rendering mappa o logica Leaflet; il Blade deve mostrare un piccolo readout visivo di latitudine, longitudine e indirizzo reverse-geocodificato usando classi Filament/Tailwind coerenti, e un messaggio chiaro quando non c’è ancora alcuna posizione impostata; il web component Lit deve essere scritto da zero in modo chiaro e minimalista, deve usare LitElement, reactive properties per latitude, longitude, zoom ed eventualmente expanded/fullscreen pseudo-mode, deve integrare Leaflet in Light DOM se necessario per compatibilità reale con Leaflet e CSS/eventi, quindi se scegli createRenderRoot() { return this; } devi documentare chiaramente che è una scelta tecnica intenzionale e non un errore, non devi accoppiare Leaflet a un id hardcoded del div della mappa ma a una class locale come .coordinate-picker-map cercata tramite this.querySelector('.coordinate-picker-map'), devi inizializzare la mappa in firstUpdated(), devi configurare correttamente le icone di default di Leaflet importando marker-icon.png, marker-icon-2x.png e marker-shadow.png, devi creare almeno un layer stradale OpenStreetMap e un layer satellitare Esri con controllo L.control.layers, devi aggiungere un marker draggable quando ci sono coordinate o quando l’utente clicca sulla mappa, devi sincronizzare marker e vista quando cambiano le proprietà latitude/longitude, devi emettere un solo evento custom coords-changed con detail { latitude, longitude }, bubbles true e composed true, devi distinguere aggiornamenti programmatici da interazioni utente per evitare loop usando un flag minimo e ben motivato come isProgrammaticUpdate, devi pulire la mappa in disconnectedCallback(), devi gestire correttamente il resize quando il componente entra in modalità espansa/fullscreen CSS chiamando map.invalidateSize(), e se implementi una modalità espansa devi chiarire che è una modalità espansa UI e non necessariamente la Fullscreen API del browser; per gli stili del web component devi scegliere un approccio coerente col vincolo Light DOM e con Leaflet: se usi injectStyles() globale fallo in modo idempotente, semplice e documentato, senza duplicare più volte i fogli stile, altrimenti proponi un’alternativa compatibile col bundler del progetto, ma non usare Shadow DOM solo per “pulizia” se poi rompe o complica Leaflet; devi evitare assolutamente questi errori architetturali: usare namespace o view path non Filament-coerenti, usare un id interno come target della mappa invece di una class, usare $wire.set al posto di $wire.$set nel bridge Livewire v4, duplicare lo stato della mappa in Alpine più del necessario, far conoscere al web component dettagli di Livewire o Filament, usare path sibling calcolati a mano per latitude/longitude nel Blade, introdurre loop tra update mappa e update Livewire, rimuovere silenziosamente dati in extractCoordinates() quando il field non è un array, usare naming fuorviante come defaultZoom quando il valore è una vera configurazione del componente, e sporcare il field con responsabilità di business non sue; devi produrre tutti i file completi e coerenti tra loro: 1) il file PHP CoordinatePicker.php con namespace corretto Modules\Geo\Filament\Forms\Components, 2) la Blade coordinate-picker.blade.php con x-data minimale e bridge Livewire/Alpine, 3) il file JS del web component coordinate-picker-field.js completo di Lit + Leaflet, 4) un esempio di utilizzo in una Resource o Wizard con CoordinatePicker::make('coordinates')->zoom(15)->center(...), 5) un esempio di mapping finale tramite CoordinatePicker::extractCoordinates(...), 6) una suite Pest minima ma seria che testi almeno extractCoordinates() nei casi normali, null, stringhe castate a float, custom column names, idempotenza su input senza field, gestione field non array, zoom configurato e di default, center configurato, nomi colonna di default e custom; devi anche scrivere una breve documentazione tecnica nel modulo Geo spiegando l’architettura, il contratto dati del field, come si usa, perché si usa Light DOM per Leaflet, perché Alpine è solo un bridge, come avviene il mapping finale verso latitude e longitude, e quali sono i limiti/estensioni future; infine devi verificare che il codice sia coerente con Filament v5, Livewire v4 e Lit 3, che non introduca warning evidenti, che sia leggibile e modulare, e devi spiegare brevemente le scelte architetturali principali prima di mostrare i file finali.
 

 bmad-create-story Agisci come un senior architect e senior full-stack engineer specializzato in Laravel 12, Filament v5, Livewire v4, Alpine.js, Lit 3, Leaflet, clean architecture, moduli Laraxot e code quality enterprise, e crea o rifattorizza da zero in modo coordinato e coerente tutti i componenti mappa del modulo Geo, assumendo che tu non sappia nulla del progetto se non ciò che sto per dirti, quindi devi seguire rigorosamente queste istruzioni, studiare bene le fonti ufficiali e implementare tutto in modo production-ready, ultra DRY, ultra KISS, ultra clean code, tipizzato, difendibile in code review, con responsabilità ben separate tra PHP field, Blade view, Alpine bridge, web component Lit e Leaflet; prima di scrivere codice studia attentamente e usa come riferimento primario queste fonti ufficiali: Filament custom fields https://filamentphp.com/docs/5.x/forms/custom-fields
 Filament forms overview https://filamentphp.com/docs/5.x/forms/overview
 Livewire 4 JavaScript https://livewire.laravel.com/docs/4.x/javascript
 Livewire 4 properties https://livewire.laravel.com/docs/4.x/properties
 Livewire 4 wire:model https://livewire.laravel.com/docs/4.x/wire-model
 Lit overview https://lit.dev/docs/
 Lit reactive properties https://lit.dev/docs/components/properties/
 Lit events https://lit.dev/docs/components/events/
 Lit styles https://lit.dev/docs/components/styles/
 Leaflet reference https://leafletjs.com/reference.html
 Leaflet quick start https://leafletjs.com/examples/quick-start/
 e se qualche punto è poco chiaro devi anche cercare e verificare le limitazioni reali di Leaflet con Shadow DOM prima di prendere decisioni sul render root del web component; il progetto usa convenzioni Laraxot, quindi devi rispettare rigorosamente namespace, path e separazione dei layer: tutti i componenti Filament devono stare sotto namespace Modules\Geo\Filament\Forms\Components e non Modules\Geo\Forms\Components, i file PHP devono stare in laravel/Modules/Geo/app/Filament/Forms/Components/, le Blade in laravel/Modules/Geo/resources/views/filament/forms/components/, il JS dei web component in laravel/Modules/Geo/resources/js/components/ o path equivalente coerente col progetto, senza mischiare codice Filament-specific nel JS; devi non solo creare o rifare il nuovo componente principale CoordinatePicker, ma devi anche migliorare e riallineare architetturalmente gli altri componenti esistenti o collegati LatitudeLongitudeInput, MapPicker, PlacePicker e CoordinatePicker, eliminando duplicazioni, incoerenze, naming fuorvianti, bridge diversi tra loro e logiche sparse, con l’obiettivo di ottenere una famiglia coerente di componenti mappa/geografia che condivida il più possibile il core UI e il contratto dati, mantenendo DRY e KISS come vincolo assoluto; devi partire da un’analisi critica dell’esistente e individuare tutto ciò che è sbagliato o fragile: namespace errati, view path errati, duplicazione tra stato del field e sibling paths latitude/longitude, abuso di entangle, troppi watcher Alpine, stato duplicato in Alpine che dovrebbe stare nel web component, bridge Livewire non documentato, uso di $wire.set al posto di $wire.$set in Livewire v4, uso di id hardcoded per agganciare la mappa invece di una class locale, scelte non motivate su Shadow DOM, listener globali non puliti, css injection non idempotente, naming imprecisi come MapPicker quando il valore reale è una coordinata o una place/location, geolocalizzazione automatica non controllata, troppe chiamate Livewire durante drag o aggiornamenti frequenti della mappa, e ogni altra fragilità che trovi; il componente principale da creare o rifattorizzare deve chiamarsi CoordinatePicker, da usare nelle Resource e nei Wizard con sintassi CoordinatePicker::make('coordinates'), e deve gestire internamente uno stato composito del field del tipo { latitude: float|null, longitude: float|null } oppure, dove necessario per PlacePicker, un contratto estendibile ma sempre minimale e stabile, senza salvare JSON nel database: il database reale continua ad avere due colonne separate latitude e longitude e il field deve essere dehydrated(false), con mapping esplicito finale verso le colonne reali tramite una utility statica extractCoordinates(array $data, string $fieldName = 'coordinates', string $latitudeColumn = 'latitude', string $longitudeColumn = 'longitude'): array; il field PHP CoordinatePicker deve estendere Filament\Forms\Components\Field, deve avere configurazione fluente pulita e tipizzata almeno per latitudeColumn(), longitudeColumn(), zoom(), center(), reverseGeocoding(), eventualmente height() se serve in modo coerente, getters corrispondenti, deve valorizzare lo state iniziale in afterStateHydrated() leggendo prima eventuali colonne reali del record e altrimenti eventuali coordinate di centro configurate, deve usare dehydrateStateUsing solo se davvero necessario ma preferibilmente restare con dehydrated(false) e mapping finale esplicito, deve includere #[ExposedLivewireMethod] su reverseGeocode(float $latitude, float $longitude): ?string e, dato che il metodo restituisce solo un dato a JavaScript senza richiedere re-render Livewire, deve usare anche #[Renderless], con reverse geocoding server-side opzionale via Nominatim o provider equivalente robusto, timeout prudente, User-Agent valorizzato e gestione errori rigorosa; il Blade field deve usare x-dynamic-component con :component="$getFieldWrapperView()" e :field="$field", leggere lo state del field con $getState() e i metadati minimi con $getStatePath(), $getKey(), $getZoom(), eventuale altezza e reverseGeocoding, costruire una config JSON minima da passare ad Alpine, usare Alpine solo come bridge sottilissimo e non come secondo source of truth UI, quindi Alpine deve fare solo queste cose: trovare il custom element nel DOM, passargli zoom e coordinate iniziali, ascoltare $wire.$watch(config.statePath, ...) per eventuali aggiornamenti server-side, ascoltare l’evento custom emesso dal web component, chiamare this.$wire.$set(config.statePath, { latitude, longitude }, false) usando l’API documentata di Livewire v4, e opzionalmente chiamare this.$wire.callSchemaComponentMethod(config.fieldKey, 'reverseGeocode', { latitude, longitude }) per valorizzare un readout testuale dell’indirizzo; devi stare molto attento a non fare troppe chiamate Livewire: durante il drag del marker o altri aggiornamenti ad alta frequenza non devi mandare una raffica continua di update al backend, quindi devi progettare il flusso in modo che l’aggiornamento verso Livewire avvenga solo in momenti sensati come dragend, click finale, oppure con una strategia minima e robusta che eviti spam di chiamate e loop reattivi, senza però compromettere la UX; tutte le mappe della famiglia di componenti, quindi LatitudeLongitudeInput, MapPicker, PlacePicker e CoordinatePicker, devono avere un marker spostabile e quando il marker viene spostato devono aggiornarsi le coordinate di latitudine e longitudine in modo coerente, mantenendo il backend sincronizzato con parsimonia e senza fare troppe chiamate Livewire; tutte le mappe devono inoltre avere sempre il tasto per entrare/uscire dalla modalità espansa/full screen e il tasto per centrare sulla posizione corrente dell’utente, con gestione robusta di geolocalizzazione del browser, fallback prudenti, e invalidateSize della mappa quando cambia la dimensione del contenitore; devi implementare il web component Lit da zero o rifattorizzarlo in modo coerente per diventare il core UI riusabile di tutti questi picker, usando LitElement, reactive properties pulite, eventi custom semanticamente chiari, e integrando Leaflet in Light DOM se necessario per compatibilità reale con Leaflet e CSS/eventi, quindi se scegli createRenderRoot() { return this; } devi documentare chiaramente che è una scelta tecnica intenzionale dovuta alla compatibilità con Leaflet e non un errore, non devi agganciare Leaflet a un id hardcoded di un div interno ma a una class locale come .coordinate-picker-map cercata tramite this.querySelector('.coordinate-picker-map'), devi inizializzare la mappa in firstUpdated(), configurare correttamente le icone di default di Leaflet importando marker-icon.png, marker-icon-2x.png e marker-shadow.png, creare almeno un layer stradale OpenStreetMap e un layer satellitare Esri con controllo layers se la feature è mantenuta, aggiungere un marker draggable quando ci sono coordinate o quando l’utente clicca sulla mappa, sincronizzare marker e vista quando cambiano le proprietà latitude/longitude, emettere un solo evento custom pulito come coords-changed con detail { latitude, longitude }, bubbles true e composed true, distinguere aggiornamenti programmatici da interazioni utente per evitare loop usando il minimo indispensabile di flag e logica difensiva, pulire mappa e risorse in disconnectedCallback(), gestire correttamente invalidateSize() quando il componente entra/esce dalla modalità espansa e quando il bottone “centra posizione corrente” modifica il view state; per gli stili del web component devi scegliere un approccio coerente col vincolo Light DOM e con Leaflet: se usi injectStyles() globale fallo in modo idempotente, semplice e documentato, senza duplicare più volte i fogli stile, altrimenti proponi un’alternativa compatibile col bundler del progetto, ma non usare Shadow DOM solo per “pulizia” se poi rompe o complica Leaflet; il bottone full screen o modalità espansa deve essere presente in tutte le mappe e chiaramente accessibile, con titolo e aria-label coerenti, e al cambio stato la mappa deve ridisegnarsi correttamente; il bottone “centra con posizione corrente” deve chiedere la geolocalizzazione del browser solo quando l’utente lo richiede esplicitamente, non automaticamente all’avvio, e deve in caso di successo centrare la mappa, posizionare o spostare il marker e aggiornare le coordinate in modo coerente, idealmente emettendo l’evento finale o eseguendo lo stesso flusso che useresti per un click/drag completato; devi evitare assolutamente questi errori architetturali: usare namespace o view path non Filament-coerenti, usare un id interno come target della mappa invece di una class, usare $wire.set al posto di $wire.$set nel bridge Livewire v4, duplicare lo stato della mappa in Alpine più del necessario, far conoscere al web component dettagli di Livewire o Filament, usare path sibling calcolati a mano per latitude/longitude nel Blade, introdurre loop tra update mappa e update Livewire, fare troppe chiamate Livewire durante drag o update continui, rimuovere silenziosamente dati in extractCoordinates() quando il field non è un array, usare naming fuorviante come defaultZoom quando il valore è una vera configurazione del componente, e sporcare i field con responsabilità di business non loro; devi produrre tutti i file completi e coerenti tra loro: 1) il file PHP CoordinatePicker.php con namespace corretto Modules\Geo\Filament\Forms\Components, 2) la Blade coordinate-picker.blade.php con x-data minimale e bridge Livewire/Alpine, 3) il file JS del web component coordinate-picker-field.js completo di Lit + Leaflet + marker draggable + bottone espandi + bottone centra posizione corrente, 4) il refactor o riallineamento dei componenti LatitudeLongitudeInput, MapPicker e PlacePicker per convergere sulla stessa architettura e ridurre duplicazioni, 5) un esempio di utilizzo in una Resource o Wizard con CoordinatePicker::make('coordinates')->zoom(15)->center(...), 6) un esempio di mapping finale tramite CoordinatePicker::extractCoordinates(...), 7) una suite Pest minima ma seria che testi almeno extractCoordinates() nei casi normali, null, stringhe castate a float, custom column names, idempotenza su input senza field, gestione field non array, zoom configurato e di default, center configurato, nomi colonna di default e custom, 8) documentazione tecnica nel modulo Geo che spieghi l’architettura comune dei picker, il contratto dati, il core condiviso, il perché del bridge minimo Alpine, il perché del Light DOM se adottato, il mapping finale verso latitude e longitude, e i limiti/estensioni future; devi rifattorizzare il tutto con l’obiettivo esplicito di avere il minor numero possibile di implementazioni diverse per le mappe, quindi se puoi estrarre un core o una base comune tra LatitudeLongitudeInput, MapPicker, PlacePicker e CoordinatePicker devi farlo, ma senza overengineering e senza creare astrazioni premature inutili: DRY e KISS prima di tutto; infine devi verificare che il codice sia coerente con Filament v5, Livewire v4 e Lit 3, che non introduca warning evidenti, che sia leggibile e modulare, e devi spiegare brevemente le scelte architetturali principali prima di mostrare i file finali.