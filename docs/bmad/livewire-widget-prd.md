---
title: "PRD — Geo Livewire"
type: prd
module: Geo
related:
  - ./livewire-inventory.md
---

# PRD Geo

### FR-G001 [MUST] `Http/Livewire/Test` assente (PHP, vista, `.test`, voce `_components.json`).
### FR-G002 [MUST] Nessun `TestWidget` dashboard.
### FR-G003 [MUST] `FormSearchAddressCategories` non diventa KPI widget (Cluster C; dettagli in [livewire-inventory.md](./livewire-inventory.md)).
### FR-G004 [SHOULD] `_components.json` coerente col disco dopo il ritiro (audit 2026-09-21: contiene solo `test`).
