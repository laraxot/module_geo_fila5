---
id: geo-phpstan-static-call-getformschema-fix
slug: geo-phpstan-static-call-getformschema-fix
title: "Geo module — PHPStan static call su AddressForm::getFormSchema() (2 errori)"
type: story
module: Geo
scope: [module:Geo, project:base_workorder_fila5]
status: done
priority: High
created: 2026-09-07
tags: [phpstan, geo, method.staticCall, filament, schemas]
---

# Geo — PHPStan static call fix

Riferimenti correlati (story precedenti sullo stesso modulo, non cancellate, questa
le aggiorna): `Modules/Geo/docs/stories/phpstan-Geo-fix.md`,
`Modules/Geo/docs/stories/phpstan-batch.story.md`,
`Modules/Geo/docs/stories/phpstan-fix-1788762467.story.md` (stub vuoti, mai
completati da chi li ha creati).

## Contesto

Campagna "PHPStan zero" (`bashscripts/docs/prompts/03-quality-gates.md`). Ultimo
numero noto in `docs/chat/phpstan-modules-zero.md` per Geo era **105 errori**
(nessun owner). Al momento di questo task, uno swarm parallelo (Epic 4,
"filament-canonical-structure") aveva già in corso l'estrazione Schemas/Tables sulle
Resource `AddressResource`/`LocationResource` (working tree con ~50 file `.md`/
Resources modificati non miei, non toccati da questa story). Uno scan isolato fresco
ha mostrato che quel lavoro concorrente aveva già silenziosamente ridotto gli errori
reali del modulo da 105 a **2**.

## Problema

`./vendor/bin/phpstan analyse Modules/Geo --memory-limit=-1` (cache pulita): **2
errori**, entrambi `method.staticCall`:

1. `app/Filament/Forms/Components/AddressField.php:41`
2. `app/Filament/Forms/Components/AddressesField.php:100`

Entrambi chiamavano `AddressForm::getFormSchema()` in forma statica.
`Modules\Xot\...\Schemas\XotBaseResourceForm::getFormSchema()` è dichiarato
`abstract public function` (istanza, mai `static`) — contratto corretto e
volutamente non statico (vedi memoria `pattern_xotbase_schemas_wrong_namespace_fatal`
e la memoria collegata `xotbaseresource-inline-delegate-static-call-bug.md`).
`AddressField`/`AddressesField` sono componenti Filament riutilizzabili
(`XotBaseSection`/`XotBaseRepeater`), non la Resource proprietaria dello schema: la
chiamata è riuso legittimo e raggiunto a runtime da `setUp()`, non un override-ponte
morto da cancellare.

## Root cause

Chiamata statica su un metodo di istanza astratto — bug meccanico introdotto durante
una migrazione precedente che ha reso `getFormSchema()` non-statico su
`XotBaseResourceForm`/`AddressForm` senza aggiornare i due consumer esterni.

## Fix

```php
// Prima
$baseSchema = AddressForm::getFormSchema();

// Dopo
$baseSchema = app(AddressForm::class)->getFormSchema();
```

Applicato identico in entrambi i file. Nessun `@phpstan-ignore`, nessun cast, nessuna
modifica a `phpstan.neon` (di sola proprietà utente), nessun `mixed` introdotto.
Pattern verificato contro l'uso già esistente nel modulo Activity
(`app(SnapshotForm::class)->getFormSchema()` nei test).

Corretto anche un bug pre-esistente e bloccante in `Modules/Geo/phpunit.xml`:
`bootstrap="vendor/autoload.php"` (relativo alla cwd, sbagliato) →
`bootstrap="../../vendor/autoload.php"` (relativo al file, come già in
Notify/Xot/UI/Lang/Tenant/Gdpr) — senza questo fix `./vendor/bin/pest -c
Modules/Geo/phpunit.xml` non parte affatto ("Cannot open bootstrap script").
Necessario per eseguire il quality gate Pest richiesto da questa stessa story, non
tema di lavoro parallelo di altri agenti.

## File toccati

- `app/Filament/Forms/Components/AddressField.php` (1 riga)
- `app/Filament/Forms/Components/AddressesField.php` (1 riga)
- `phpunit.xml` (bootstrap path)
- `docs/coverage.md` (aggiornato prima/dopo)
- `docs/stories/01.Geo-phpstan-fix.story.md` (questo file)
- `bashscripts/ai/wiki/memories/geo-addressform-schema-reuse-static-call-bug.md` (nuova nota)

Non toccati (WIP di un altro agente, non miei da validare/committare):
`AddressResource.php`, `AddressResource/Pages/ViewAddress.php`,
`AddressResource/Schemas/AddressForm.php`, `AddressResource/Schemas/AddressInfolist.php`,
`LocationResource.php` e pagine/schemi collegati, tutti i `.md` sotto `docs/` diversi
da `coverage.md` e questa story.

## Verifica

- [x] `./vendor/bin/phpstan analyse Modules/Geo --memory-limit=-1` (cache pulita):
      **105 → 2 → 0 errori**.
- [x] `declare(strict_types=1);` presente in entrambi i file toccati (non modificato,
      già presente).
- [x] Nessun riferimento diretto a `User`/`Profile` introdotto (n/a, file non li
      usano).
- [x] `./tools/phpmd.sh Modules/Geo/app/Filament/Forms/Components/AddressField.php text Modules/Geo/phpmd.ruleset.xml`
      e l'equivalente per `AddressesField.php`: nessun nuovo finding introdotto dal
      diff (verificato riga per riga contro `git diff`); i 2 finding esistenti
      (`BooleanArgumentFlag` su `disableLiveUpdates`, `UnusedLocalVariable $address`
      in un metodo diverso) sono pre-esistenti, fuori scope.
- [x] `./tools/phpinsights.sh` — NON eseguito: non installato in questo repo
      (rimosso, incompatibile con Pest 5 — vedi memoria second-brain
      `pest5-incompatibile-con-phpinsights`), confermato dall'hook di verifica
      post-edit di questa sessione. Non è un blocco imputabile a questa story.
- [x] `./vendor/bin/pest Modules/Geo/tests -c Modules/Geo/phpunit.xml --no-coverage`:
      test mirato `FilamentComponentsTest.php::"AddressField can be instantiated"`
      passa (instanzia `AddressField::make('address')`, esercita
      `getAddressFormSchema()` → `app(AddressForm::class)->getFormSchema()` a
      runtime, non solo staticamente). Suite completa del modulo: numerosi
      fallimenti pre-esistenti e non correlati (chiamate a Google Maps/Bing
      Maps/Mapbox reali senza mock di rete, `GeoMapWidgetTest` blade,
      messaggi di eccezione disallineati) — già documentati in
      `docs/coverage.md` prima di questo fix, non regressioni introdotte da
      questa story. Vedi `docs/coverage.md` per il conteggio aggiornato.
- [x] Coverage: non ridotto (vedi `docs/coverage.md`).
- [x] BMAD story aggiornata (questo file), nessuna story esistente cancellata o
      rinumerata.
