<?xml version="1.0"?>
<ruleset name="Custom_PHPMD"
         xmlns="http://pmd.sf.net/ruleset/1.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sf.net/ruleset/1.0.0
                     http://pmd.sf.net/ruleset_xml_schema.xsd"
         xsi:noNamespaceSchemaLocation="
                     http://pmd.sf.net/ruleset_xml_schema.xsd">
    <description>Custom ruleset for Laraxot project</description>

    <!-- Exclude patterns for tests, migrations, and specific config files -->
    <exclude-pattern>.*/database/migrations/.*</exclude-pattern>
    <exclude-pattern>.*/tests/.*</exclude-pattern>
    <exclude-pattern>.*/.php-cs-fixer.dist.php</exclude-pattern>
    <exclude-pattern>.*/_ide_helper.php</exclude-pattern>
    <exclude-pattern>.*/vendor/.*</exclude-pattern>

    <!-- Clean Code Rules -->
    <rule ref="rulesets/cleancode.xml">
        <exclude name="StaticAccess"/>
    </rule>

    <!-- Code Size Rules -->
    <rule ref="rulesets/codesize.xml"/>

    <!-- Controversial Rules - Keep all BUT CamelCase rules -->
    <rule ref="rulesets/controversial.xml">
        <exclude name="CamelCaseClassName"/>
        <exclude name="CamelCasePropertyName"/>
        <exclude name="CamelCaseMethodName"/>
        <exclude name="CamelCaseParameterName"/>
        <exclude name="CamelCaseVariableName"/>
    </rule>

    <!-- Design Rules -->
    <rule ref="rulesets/design.xml"/>

    <!-- Naming Rules -->
    <rule ref="rulesets/naming.xml">
        <!-- Allow short variables like $id, $q, $to etc. -->
        <properties>
            <property name="minimum" value="2"/>
        </properties>
    </rule>
    <rule ref="rulesets/naming.xml/ShortVariable">
        <properties>
            <property name="minimum" value="2"/>
        </properties>
    </rule>


    <!-- Unused Code Rules -->
    <rule ref="rulesets/unusedcode.xml"/>

</ruleset>
